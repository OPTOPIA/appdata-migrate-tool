# AppDataMigrateTool 开发计划

> 本文是面向后续 AI / Vibe Coding 工具的项目上下文与实施清单。执行任务前应先阅读本文、`readme.md` 和 `AppDataMigrate.ps1`。默认原则是数据安全优先，不在缺少测试的情况下重写核心迁移流程。

## 1. 项目目标

这是一个仅面向 Windows 的目录迁移工具。它把 C 盘上的用户目录复制到 D 盘，在原位置保留 `.bak` 备份，并创建 NTFS Junction，使原路径透明地指向新位置；同时提供回滚入口。

当前典型用途是迁移 `C:\Users\<User>\AppData\...`，但脚本实际允许迁移大多数非系统 C 盘目录。当前目标路径固定为：

```text
D:\C_Data_Redirect\<源路径相对于 C:\ 的部分>
```

例如：

```text
C:\Users\Alice\AppData\Local
=> D:\C_Data_Redirect\Users\Alice\AppData\Local
```

## 2. 当前文件与职责

| 文件 | 职责 |
| --- | --- |
| `AppDataMigrate.ps1` | 核心逻辑：校验、复制、验证、备份、创建 Junction、计划清理、回滚 |
| `AppDataMigrate.cmd` | 管理员检查、读取源路径、调用迁移模式 |
| `AppDataRollback.cmd` | 管理员检查、读取源路径、调用回滚模式 |
| `readme.md` | 面向用户的简要说明 |

当前已具备 Pester 测试、Windows GitHub Actions CI、验证脚本、版本号和变更记录；许可证选择及真实 NTFS 管理员集成测试仍待完成。

## 3. 当前流程

### 3.1 迁移

1. 检查管理员权限。
2. 解析并规范化已存在的源目录。
3. 限制源目录必须位于 C 盘，并拒绝 C 盘根目录、链接及部分系统目录。
4. 根据源路径计算 D 盘目标目录和 `<源目录>.bak`。
5. 要求目标和备份均不存在，并请求用户确认。
6. 使用 `robocopy /E /COPY:DAT /XJ /R:1 /W:1` 复制数据。
7. 以文件数和总字节数比较源、目标。
8. 把源目录重命名为 `.bak`。
9. 在源路径创建指向目标目录的 Junction；失败时尝试恢复 `.bak`。
10. 创建两天后由 SYSTEM 删除 `.bak` 的一次性计划任务。

### 3.2 回滚

1. 检查 `.bak` 存在且不是链接，并检查源路径是 Junction。
2. 删除 Junction，将 `.bak` 重命名回原目录。
3. 询问是否递归删除 D 盘目标数据。

## 4. 已知问题与风险

### P0：必须在扩展功能前解决

- **回滚可能丢失迁移后的数据。** 当前 `.bak` 是迁移时的旧快照。应用在迁移后写入 D 盘的新文件或修改不会进入 `.bak`；直接恢复 `.bak` 并删除目标会造成数据丢失。回滚前必须先把目标内容安全同步回备份/恢复目录，验证后再切换。
- **失败恢复不是完整事务。** 目标目录创建或复制后失败时会留下残留目录；重命名后的一些异常路径只做有限恢复。需要定义每个阶段的状态和补偿动作。
- **自动清理是破坏性操作。** 两天后无条件删除 `.bak`，没有再次验证 Junction、目标路径或迁移状态，也没有方便的取消/延期命令。

### P1：可靠性与可维护性

- README 声称 robocopy 有日志，实际命令没有 `/LOG` 或等价实现。
- `/COPY:DAT` 不复制 ACL、所有者和审计信息；需明确产品策略，并针对 AppData 场景测试 `/COPYALL` 或 `/SEC` 的影响。
- 当前校验只比较文件数量和总大小，不检查文件内容、相对路径集合、空目录、时间戳或权限。
- `Get-ChildItem -Recurse` 对大型 AppData 目录耗时、耗内存，且容易受锁定文件/访问拒绝影响。
- 系统目录黑名单不完整；仅靠字符串前缀不足以覆盖重解析点、路径别名、用户配置目录等边界情况。
- 目标盘和根目录固定为 D 盘，无法配置，也没有空间、文件系统类型、目标是否支持 Junction 等预检。
- 计划任务名称只含时间戳，缺少源路径标识；任务执行后不自删，临时脚本创建失败或任务创建失败时清理不足。
- `schtasks /sd` 日期格式可能受系统区域设置影响，需要跨区域验证或改用 PowerShell ScheduledTasks API。
- 调用 `cmd /c mklink/rmdir` 的参数转义对特殊字符路径需要专项测试。
- 文案为英文，而 README 为中文；交互与错误输出缺少统一语言和可机器读取的退出码约定。

### P2：产品体验

- 没有 `-WhatIf`、非交互模式、配置化目标根目录、状态查询、清理备份、取消清理任务等命令。
- 没有显示数据量、可用空间、预计操作和最终摘要。
- CMD 入口重复了部分校验，长期容易与 PowerShell 主逻辑不一致。

## 5. 目标架构

先保持单脚本可运行，再逐步拆分，避免过早引入 GUI 或大型框架。

```text
AppDataMigrateTool/
├─ AppDataMigrate.ps1          # 稳定的 CLI 入口
├─ AppDataMigrate.cmd          # 双击迁移入口
├─ AppDataRollback.cmd         # 双击回滚入口
├─ src/
│  └─ AppDataMigrate.Core.psm1 # 可测试的纯函数和核心操作（第二阶段再拆）
├─ tests/
│  ├─ Unit/
│  └─ Integration/
├─ scripts/
│  └─ verify.ps1               # 统一静态检查与测试入口
├─ docs/
│  ├─ DESIGN.md
│  └─ RECOVERY.md
├─ readme.md
└─ DEVELOPMENT_PLAN.md
```

建议把一次迁移建模为明确状态：

```text
Preflight -> Copying -> Verified -> SourceBackedUp -> Linked -> CleanupScheduled
                         |              |               |
                         +----补偿------ +------补偿-----+
```

状态记录建议放在目标目录旁的 JSON 清单中，至少记录工具版本、源路径、目标路径、备份路径、开始/完成时间、复制结果、验证结果、Junction 信息及计划任务名。清单不得包含隐私文件内容。

## 6. 分阶段实施计划

### 阶段 0：建立安全基线（P0）

- [x] 为现有行为补充 Pester 测试框架和临时目录测试夹具。
- [x] 把路径计算、路径包含判断、危险目录判定拆为无副作用函数并做表驱动测试。
- [x] 设计无损回滚：先将 Junction 目标同步回恢复目录，验证成功后再移除 Junction；任何失败都保留至少一份完整数据。
- [x] 明确回滚策略：目标目录为当前权威副本；回滚保留旧 `.bak` 和目标，不删除目标数据。
- [x] 为迁移各阶段实现补偿逻辑，并保证重复运行能识别“未开始、进行中、已迁移、部分失败、可回滚”。清单记录状态；Windows CI 已验证 Junction 创建前故障时恢复源目录并保留目标。
- [x] 将自动删除改为显式可取消的安全清理；执行清理前重新校验源 Junction 与清单一致。
- [x] 为所有破坏性动作增加目标边界验证，禁止删除目标根目录、盘符根目录或与清单不一致的路径。

验收标准：

- 在迁移完成后新增、修改、删除若干目标文件，再执行回滚，不丢失迁移后的有效数据。
- 在复制失败、源重命名失败、Junction 创建失败、计划任务创建失败等注入故障下，源数据始终存在且可定位。
- 重复执行同一命令不会静默覆盖或删除既有数据。

### 阶段 1：提高复制与验证可靠性（P1）

- [x] 增加目标盘存在性、NTFS 文件系统、可用空间、源目录占用和关键工具可用性预检。
- [x] 确定元数据复制策略：当前使用 `/COPY:DAT /DCOPY:DAT`，不复制 ACL、所有者或审计信息；理由与限制已记录。
- [x] 将 robocopy 输出写入按时间命名的日志，同时在控制台显示摘要。
- [x] 正确解释 robocopy 的 0–7 成功/差异码与 8+ 失败码，并保存退出信息。
- [x] 用相对路径集合、大小、时间戳进行默认验证；提供可选 SHA-256 强校验，逐文件处理而不整体读入内存。
- [x] 检测并报告被占用、跳过、拒绝访问和发生变化的文件。robocopy 日志和验证失败会报告错误；Windows CI 使用独占文件锁验证失败时源数据仍保留。
- [x] 统一错误分类与退出码，确保 CMD 和自动化调用者可判断结果。

验收标准：

- 至少覆盖空目录、隐藏文件、长路径、Unicode、空格、特殊字符、只读文件、锁定文件和嵌套 Junction。
- 日志能回答“复制了什么、跳过了什么、为何失败、数据当前在哪里”。

### 阶段 2：改善 CLI 与配置（P1/P2）

- [x] 增加 `-DestinationRoot`，默认仍为 `D:\C_Data_Redirect`，并校验不能位于源目录内部。
- [x] 增加 `-WhatIf` / `-Confirm` 语义和可选非交互模式；默认交互行为保持兼容。
- [x] 提供 `Migrate`、`Rollback`、`Status`、`Cleanup` 四类明确操作，并用参数集约束组合。
- [x] `Status` 展示 Junction 目标、备份、清单、清理任务和异常状态。
- [x] `Cleanup` 只删除已验证且与清单匹配的备份，并支持取消计划任务。
- [x] 统一中英文策略：README 与双击 `.cmd` 入口使用中文；PowerShell 核心错误文本使用稳定英文并配合数值退出码，便于自动化解析。
- [x] 精简 CMD，业务校验集中在 PowerShell 核心脚本。

验收标准：

- 命令行帮助包含完整示例，交互和非交互方式均可完成迁移、查询、回滚和清理。
- 旧的双击入口继续可用。

### 阶段 3：工程化与文档（P2）

- [x] 增加 `scripts/verify.ps1`，依次执行 PowerShell AST 解析、PSScriptAnalyzer（若已安装）和 Pester。
- [x] 配置 GitHub Actions 的 Windows runner，避免在 Linux 上模拟 Junction 行为。
- [x] 在 Windows CI 中加入隔离的真实 NTFS 迁移、无损回滚与清理集成测试。
- [x] 更新 README，使功能描述与代码一致，并补充风险、兼容性、常见故障和恢复步骤。
- [x] 编写 `docs/DESIGN.md`，记录状态机、数据不变量和安全边界。
- [x] 编写 `docs/RECOVERY.md`，说明每种中断状态下如何人工恢复。
- [x] 已增加 Apache-2.0 LICENSE、CHANGELOG 和语义化版本号。
- [x] 已评估 GUI：当前 CLI 安全基线仍在真实 NTFS 验证阶段，因此暂不引入 GUI；未来 GUI 必须调用同一核心逻辑。

## 7. 测试策略

### 单元测试

- 路径规范化及 C 盘相对路径计算。
- 大小写、尾部分隔符、空格、Unicode 和特殊字符。
- 危险路径、盘符根目录、源目标嵌套、链接和重解析点拒绝。
- 状态判断、清单读写、robocopy 退出码映射。
- 计划任务名称生成与清理边界判断。

### 集成测试

- 使用独立临时 NTFS 测试目录；不要触碰真实用户 AppData。
- 迁移成功后验证源为 Junction、目标内容正确、备份存在。
- 在目标产生新数据后回滚，验证数据无损。
- 模拟各阶段异常并验证补偿行为。
- 测试计划任务的创建、查询、取消；CI 中避免真正等待两天。

### 手工验收

- Windows 10 与 Windows 11。
- 管理员/非管理员、中文/英文区域设置。
- 小目录和大量小文件目录。
- 目标空间不足、文件被占用、安全软件干预、重启后恢复。

## 8. 开发约束（供编码工具遵循）

1. 数据安全优先于“自动修复”。无法证明删除安全时必须停止并保留数据。
2. 不直接在真实 `AppData` 上运行开发测试；只使用明确创建的临时测试目录。
3. 不改变或删除用户已有的 `.bak`、目标目录、Junction 或计划任务，除非它们由当前清单唯一标识且用户已确认。
4. 所有删除操作必须使用规范化绝对路径，并在执行前验证路径边界。
5. 核心函数应支持依赖注入或 mock，避免单元测试真的调用 `robocopy`、`mklink`、`schtasks`。
6. 每次只完成一个可验收的小任务；同时更新测试与文档。
7. 保持 Windows PowerShell 5.1 兼容，除非后续明确决定最低版本改为 PowerShell 7。
8. 不静默改变默认目标目录和旧 CMD 入口行为。
9. 不把“文件数和总大小相等”描述成内容完全一致。
10. 提交前运行统一验证脚本，并在变更说明中列出未覆盖的真实 Windows 行为。

## 9. 推荐的首批开发任务

按以下顺序实施，避免并行修改核心状态转换：

1. **TASK-001：测试基架**——加入 Pester、临时目录夹具和路径函数测试，不改变运行行为。
2. **TASK-002：状态与清单设计**——加入迁移清单 schema、读写和状态识别测试，暂不启用破坏性恢复。
3. **TASK-003：无损回滚**——实现目标数据回写、验证、切换和失败保留策略。
4. **TASK-004：事务补偿**——覆盖迁移每个失败点和重复运行。
5. **TASK-005：安全清理**——清单绑定、任务取消、执行前复验、任务自删除。
6. **TASK-006：复制日志与验证**——补齐 robocopy 日志、流式验证和错误摘要。
7. **TASK-007：CLI 参数集**——目标根目录、WhatIf、Status、Cleanup、非交互模式。
8. **TASK-008：CI 与恢复文档**——Windows CI、设计说明、人工恢复手册。

## 10. 当前基线说明

- 2026-07-14 检查时，`AppDataMigrate.ps1` 可通过 PowerShell AST 语法解析。
- 尚未在真实迁移场景执行脚本，因为这需要管理员权限并会修改文件系统和计划任务。
- 当前 Git 工作区中的四个原有文件均已有未提交修改，且仓库存在合并状态；后续工具应先检查 `git status`，不得覆盖或回退这些用户修改。
- 本文只描述分析结果和建议路线，不代表上述改进已经实现。
